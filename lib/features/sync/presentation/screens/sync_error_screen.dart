import 'package:flutter/material.dart';

class SyncErrorScreen extends StatelessWidget {
  const SyncErrorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SyncErrorScreen'),
      ),
    );
  }
}
